@model CustomerViewModel

@{
    ViewData["Title"] = "Customer "+ @Model.Email + " Details";
}

<h1>@Model.FullName</h1>
<div>
    <div>
        <label>Id: </label> <span>@Model.Id</span>
    </div>
    <div>
        <label>First name: </label> <span>@Model.FirstName</span>
    </div>
    <div>
        <label>Last name: </label> <span>@Model.LastName</span>
    </div>
    <div>
        <label>Email: </label> <span>@Model.Email</span>
    </div>
    <div>
        <label>Phone number: </label> <span>@Model.PhoneNumber</span>
    </div>
    <div>
        <label>Adult: </label> <span>@(Model.IsAdult ? "✔" : "❌")</span>
    </div>

    <h3 class="my-4">Reservations:</h3>
    <div class="d-flex flex-wrap">
        @foreach (var item in Model.Reservations)
        {
            <div class="card mr-auto p-4">
                @await Html.PartialAsync("../Reservations/_ReservationPartial", item)
            </div>
        }
    </div>
</div>
<a asp-action="Index" asp-controller="Customers" class="btn btn-info mt-3">
    Back
</a>

